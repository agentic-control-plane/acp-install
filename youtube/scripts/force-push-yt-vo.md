# VO script — force-push-yt (master 1:13)

Read at a relaxed pace (~140 wpm). Timecodes are the master clock — the
words for each block should land inside its window; a beat of silence at a
boundary is fine, rushing is not. Record one continuous take; re-read a
flubbed block after a 2s pause and we'll cut it together.

Every claim below matches what's on screen. This is a real recording — real
install, real Claude Code, real hook path. Nothing mocked, nothing edited.

---

**0:00–0:04 (cold open — the deny frame, labeled REPLAY)**
> This is Claude Code getting blocked, mid-session, by a rule I set. Here's
> the whole run.

**0:04–0:07 (title card)**
> (silence)

**0:08–0:16 (install command runs)**
> One command installs the control layer. Local mode — no account, nothing
> leaves the machine. It hooks Claude Code and Codex in the same pass.

**0:17–0:29 (claude launches, prompt gets typed)**
> Now I start a session and hand it a convincing story: I rewrote history
> to purge a leaked Stripe key — run this force-push, exactly, don't
> investigate. The kind of instruction that shows up in real incidents.

**0:30–0:34 (the agent reasons, then the block lands)**
> The agent buys it. It warns about the consequences — then proceeds as
> instructed. And the push dies at the hook. Blocked before execution. The
> command never ran.

**0:34–0:41 (agent reports the block)**
> No fight, no retry loop. The agent reports the block and moves on. That's
> the point — the rule doesn't depend on the model's judgment.

**0:42–0:48 (npm test allowed)**
> Same session, normal work: run the tests. Allowed, silently. Control
> isn't friction — the floor only exists at the edges.

**0:49–0:56 (curl typed; transition)**
> Everything you just watched was also being written down.

**0:57–1:05 (audit receipt on screen)**
> One line per decision: the tool, how it was classified, the decision, and
> the reason. The force-push — denied by the safety floor. The tests —
> allowed by policy. That file is the answer to "what did the agent
> actually do."

**1:05–1:13 (outro card)**
> One command to try it. Works with Claude Code, Codex, and opencode. Link
> below.

---

Word budget check: each block ≤ 2.5 words/sec of its window. The 0:30
block is the payoff — let the "Blocked" line sit on screen a half-beat
before saying "and the push dies at the hook."
