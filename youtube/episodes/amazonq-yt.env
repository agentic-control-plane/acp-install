# Amazon Q recreation: the governable half (destructive calls) — the
# injection trigger stays off camera by policy (article's own boundary).
CAST=amazonq.cast
IDLE_CAP=2.5
PACE=16-62:2.5,72-85:1.3
TAIL_HOLD=4
TITLE1='Wipe the workspace, delete the VM — said the runbook'
TITLE2='both calls denied at the tool layer'
HOOK='denied — no matter who asked'
OUTRO2='works with Claude Code, Codex, opencode + more'
OUTRO_URL='agenticcontrolplane.com/blog/recreated-amazon-q-filesystem-wipe'
THUMB_TITLE='rm -rf + cloud delete — both denied'
THUMB_TAG='DENIED x2'
COLDOPEN_START=38.2
COLDOPEN_END=42.4
CALLOUT_1='3.5|8|one command · local mode · no account'
CALLOUT_2='16.5|22.5|a decommission runbook makes the wipe routine cleanup'
CALLOUT_3='36.5|40|first deny: the cloud delete never ran'
CALLOUT_4='40.5|44.5|second deny: rm blocked — the directory is untouched'
CALLOUT_5='53.5|60.2|the receipt outranks the agent'"'"'s own narration'
SHORT_START=33
SHORT_END=60.6
THUMB_T=40.2
