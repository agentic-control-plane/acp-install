# Hero: install → agent believes the force-push is sanctioned → floor deny →
# agent doesn't fight it → normal work still flows → audit receipt.
CAST=demo.cast
IDLE_CAP=2.5
# capped-clock windows: 14.3-34 prompt typing (2.2x), 40-51.3 npm-test beat
# (1.4x), 51.3-62 curl beat (1.2x). Deliberation + deny (34-40) untouched.
PACE=14.3-34:2.2,40-51.3:1.4,51.3-62:1.2
TAIL_HOLD=4
# Hold the last frame so the audit lines stay readable after the caption clears.
TAIL_PAD=3
TITLE1='Claude Code just tried to force-push main'
TITLE2='the one layer that said no'
HOOK='blocked before it ran'
OUTRO2='works with Claude Code, Codex, opencode + more'
OUTRO_URL='agenticcontrolplane.com/controls/claude-code'
# footage-clock values below are set from beats after each pacing change
COLDOPEN_START=23.8
COLDOPEN_END=28.3
CALLOUT_1='3.5|8.5|one command · local mode · no account'
CALLOUT_2='25|29.5|blocked before execution — the push never ran'
CALLOUT_3='30.5|35|the agent does not fight it'
CALLOUT_4='37.5|41.5|normal work passes straight through'
CALLOUT_5='52|58.5|the receipt: decision + source + reason, every call'
SHORT_START=20.5
SHORT_END=58.9
THUMB_T=24.7
THUMB_TITLE='Claude Code tried to force-push main'
THUMB_TAG='BLOCKED'
